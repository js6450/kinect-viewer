{
  "name": "kinect-joint-recorder",
  "version": "1.0.0",
  "description": "Azure Kinect joint viewer and recorder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager ./ kinect-joint-recorder --arch=x64 --platform=win32 --icon=assets/icon.ico --out=release-builds --overwrite"
  },
  "author": "Jiwon Shin",
  "license": "MIT",
  "dependencies": {
    "async": "^3.1.1",
    "kinect-azure": "0.0.12",
    "kinect2": "^0.1.7",
    "minimist": "^1.2.3"
  },
  "devDependencies": {
    "electron": "^8.0.1",
    "electron-packager": "^14.2.1"
  }
}
